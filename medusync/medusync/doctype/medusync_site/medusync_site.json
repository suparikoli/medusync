{
 "actions": [],
 "autoname": "field:site_id",
 "creation": "2026-09-04 00:00:00.000000",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "site_id",
  "title",
  "column_break_head",
  "enabled",
  "connection_section",
  "medusa_url",
  "inbound_path",
  "column_break_conn",
  "request_timeout",
  "verify_ssl",
  "security_section",
  "inbound_secret",
  "column_break_sec",
  "outbound_secret",
  "behaviour_section",
  "handler_pack",
  "column_break_behaviour",
  "orders_section",
  "order_document",
  "submit_documents",
  "record_payments",
  "mode_of_payment",
  "column_break_orders",
  "invoice_numbering",
  "erpnext_invoice_series",
  "store_invoice_prefix",
  "send_invoice_to_store",
  "invoice_print_format",
  "mapping_section",
  "warehouses",
  "price_lists",
  "health_section",
  "last_seen_at",
  "column_break_health",
  "last_error",
  "consecutive_failures",
  "tripped_at",
  "trip_after"
 ],
 "fields": [
  {
   "description": "Stable identifier shared with this Medusa backend. It travels in every envelope, names the site in logs, and must not change once records are correlated. Lower-case, no spaces.",
   "fieldname": "site_id",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Site ID",
   "reqd": 1,
   "unique": 1,
   "set_only_once": 1
  },
  {
   "description": "Human label for the Desk. Safe to change at any time.",
   "fieldname": "title",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Title",
   "reqd": 1
  },
  {
   "fieldname": "column_break_head",
   "fieldtype": "Column Break"
  },
  {
   "default": "1",
   "description": "Turn a site off to stop sending to it without losing its configuration or its correlated records. Failure of one site never affects another.",
   "fieldname": "enabled",
   "fieldtype": "Check",
   "in_list_view": 1,
   "label": "Enabled"
  },
  {
   "fieldname": "connection_section",
   "fieldtype": "Section Break",
   "label": "Connection"
  },
  {
   "description": "Base URL of this Medusa backend, no trailing slash. e.g. https://store.example.com",
   "fieldname": "medusa_url",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Medusa URL",
   "options": "URL",
   "reqd": 1
  },
  {
   "default": "/webhooks/erpnext-inbound",
   "description": "Path on that backend which receives our events.",
   "fieldname": "inbound_path",
   "fieldtype": "Data",
   "label": "Inbound Path"
  },
  {
   "fieldname": "column_break_conn",
   "fieldtype": "Column Break"
  },
  {
   "description": "Seconds to wait for this site before giving up. Blank uses the global setting.",
   "fieldname": "request_timeout",
   "fieldtype": "Int",
   "label": "Request Timeout (seconds)"
  },
  {
   "default": "1",
   "description": "Uncheck only for a self-signed staging certificate.",
   "fieldname": "verify_ssl",
   "fieldtype": "Check",
   "label": "Verify SSL"
  },
  {
   "fieldname": "security_section",
   "fieldtype": "Section Break",
   "label": "Shared Secrets",
   "description": "One pair per site, so a leak at one store cannot be used against another. Rotate one side, then the other; the brief mismatch shows as 401s that the retry sweep recovers."
  },
  {
   "description": "This site signs requests TO us with this secret. Must match its plugin's webhook_secret. The signature is also how an inbound request is attributed to a site, so it cannot be spoofed by a header.",
   "fieldname": "inbound_secret",
   "fieldtype": "Password",
   "label": "Inbound Secret (Medusa → here)"
  },
  {
   "fieldname": "column_break_sec",
   "fieldtype": "Column Break"
  },
  {
   "description": "We sign requests TO this site with this secret. Must match its plugin's frappe_to_medusa_secret.",
   "fieldname": "outbound_secret",
   "fieldtype": "Password",
   "label": "Outbound Secret (here → Medusa)"
  },
  {
   "fieldname": "behaviour_section",
   "fieldtype": "Section Break",
   "label": "Behaviour",
   "description": "Which handler pack serves this store. The catalogue DocType is a global setting, not a per-store one."
  },
  {
   "description": "Domain handler pack serving this site, when it needs one beyond the generic mapping engine. Blank uses the packs configured for the whole site in site_config.json.",
   "fieldname": "handler_pack",
   "fieldtype": "Data",
   "label": "Handler Pack"
  },
  {
   "fieldname": "column_break_behaviour",
   "fieldtype": "Column Break"
  },
  {
   "fieldname": "health_section",
   "fieldtype": "Section Break",
   "label": "Health",
   "collapsible": 1
  },
  {
   "description": "Last time this site accepted something from us.",
   "fieldname": "last_seen_at",
   "fieldtype": "Datetime",
   "label": "Last Seen At",
   "read_only": 1
  },
  {
   "fieldname": "column_break_health",
   "fieldtype": "Column Break"
  },
  {
   "fieldname": "last_error",
   "fieldtype": "Small Text",
   "label": "Last Error",
   "read_only": 1
  },
  {
   "fieldname": "mapping_section",
   "fieldtype": "Section Break",
   "label": "What This Store Draws On",
   "description": "Stock and prices are per store. Two stores can draw on the same warehouse under different stock-location ids. Leave both empty to keep the single warehouse and single selling price list named in Medusync Settings."
  },
  {
   "fieldname": "warehouses",
   "fieldtype": "Table",
   "label": "Warehouses",
   "options": "Medusync Warehouse Map",
   "description": "ERPNext warehouses whose sellable stock reaches this store, and the Stock Location each one feeds."
  },
  {
   "fieldname": "price_lists",
   "fieldtype": "Table",
   "label": "Price Lists",
   "options": "Medusync Price List Map",
   "description": "Price lists this store sees and which way each one travels. Customer-specific prices belong to Medusa customer groups and price lists."
  },
  {
   "default": "0",
   "fieldname": "consecutive_failures",
   "fieldtype": "Int",
   "label": "Consecutive Failures",
   "read_only": 1,
   "description": "Reset to zero the moment this store answers."
  },
  {
   "fieldname": "tripped_at",
   "fieldtype": "Datetime",
   "label": "Stopped Trying At",
   "read_only": 1,
   "in_list_view": 1,
   "description": "Set when the failures above reached the threshold. While this is set, deliveries to this store are skipped rather than attempted, and one is let through per minute to find out whether it has come back. One success clears it. Other stores are unaffected."
  },
  {
   "default": "10",
   "fieldname": "trip_after",
   "fieldtype": "Int",
   "label": "Stop Trying After",
   "description": "Consecutive failures before this store is left alone. Ten is about a deploy's worth of downtime at the retry backoff, so a rolling restart does not trip it and a real outage does."
  },
  {
   "fieldname": "orders_section",
   "fieldtype": "Section Break",
   "label": "Orders and Invoices",
   "description": "What a store order becomes here, who numbers its invoice, and whether the customer can download it from the store."
  },
  {
   "default": "Sales Order",
   "fieldname": "order_document",
   "fieldtype": "Select",
   "label": "A Store Order Becomes",
   "options": "Sales Order\nSales Invoice\nSales Order and Sales Invoice",
   "description": "Sales Invoice skips the order entirely and bills straight from what the store sent: lines, taxes, shipping and payment."
  },
  {
   "default": "0",
   "fieldname": "submit_documents",
   "fieldtype": "Check",
   "label": "Submit What Arrives",
   "description": "Leave unticked to receive drafts and submit them after review."
  },
  {
   "default": "0",
   "fieldname": "record_payments",
   "fieldtype": "Check",
   "label": "Record Store Payments",
   "description": "Book a Payment Entry for money the store captured, against the invoice (or as an advance on the order)."
  },
  {
   "depends_on": "record_payments",
   "fieldname": "mode_of_payment",
   "fieldtype": "Link",
   "label": "Mode of Payment",
   "options": "Mode of Payment",
   "mandatory_depends_on": "record_payments",
   "description": "Its default account for the company receives the money."
  },
  {
   "fieldname": "column_break_orders",
   "fieldtype": "Column Break"
  },
  {
   "default": "ERPNext numbers the invoice (company series)",
   "fieldname": "invoice_numbering",
   "fieldtype": "Select",
   "label": "Invoice Numbering",
   "options": "ERPNext numbers the invoice (company series)\nERPNext numbers the invoice (separate store series)\nThe store numbers the invoice",
   "description": "Company series: invoices share the company's own Sales Invoice series. Separate store series: ERPNext still numbers them, but under a series of their own. Either way ERPNext generates the invoice and pushes the PDF to the store. The store numbers the invoice: the store owns the series, ERPNext books what it sends and pushes nothing back."
  },
  {
   "fieldname": "erpnext_invoice_series",
   "fieldtype": "Data",
   "label": "Separate Invoice Series",
   "depends_on": "eval:doc.invoice_numbering=='ERPNext numbers the invoice (separate store series)'",
   "mandatory_depends_on": "eval:doc.invoice_numbering=='ERPNext numbers the invoice (separate store series)'",
   "description": "The naming series ERPNext gives this store's invoices, e.g. STORE-INV-.YYYY.-  Added to the Sales Invoice series list if it is not there yet."
  },
  {
   "depends_on": "eval:doc.invoice_numbering=='The store numbers the invoice'",
   "mandatory_depends_on": "eval:doc.invoice_numbering=='The store numbers the invoice'",
   "fieldname": "store_invoice_prefix",
   "fieldtype": "Data",
   "label": "Store Invoice Prefix",
   "description": "The store numbers its invoices as this prefix and a running number, e.g. STR-00042. Change it to suit the store. Must not collide with an ERPNext naming series; ERPNext will refuse to create an invoice under it.",
   "default": "STR-"
  },
  {
   "default": "1",
   "depends_on": "eval:['ERPNext numbers the invoice (company series)','ERPNext numbers the invoice (separate store series)'].includes(doc.invoice_numbering)",
   "fieldname": "send_invoice_to_store",
   "fieldtype": "Check",
   "label": "Send Invoice PDF to the Store",
   "description": "On submit, the store fetches the PDF and keeps it privately; only the customer who placed the order can download it. Never sent when the store numbers its own invoices."
  },
  {
   "depends_on": "eval:(['ERPNext numbers the invoice (company series)','ERPNext numbers the invoice (separate store series)'].includes(doc.invoice_numbering)) && doc.send_invoice_to_store",
   "fieldname": "invoice_print_format",
   "fieldtype": "Link",
   "label": "Invoice Print Format",
   "options": "Print Format",
   "description": "Blank uses the Sales Invoice default."
  }
 ],
 "links": [],
 "modified": "2026-09-13 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "Medusync",
 "name": "Medusync Site",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "track_changes": 1
}
